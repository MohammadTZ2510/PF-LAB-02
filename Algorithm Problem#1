1. Start
2. Input two numbers: N and n
3. Check if n%N==0 (i.e., if the remainder is 0):
    If true, N is a divisor of n
    If false, N is not a divisor of n
4. If N is a divisor, check:
    If N%2==0, print "Nth number is even"
    Else, print "Nth number is odd"
5. End
